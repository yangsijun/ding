=== Homebrew Formula ===
class Ding < Formula
  desc "CLI tool that sends push notifications to iPhone/Apple Watch when terminal commands complete"
  homepage "https://github.com/sijunyang/ding"
  url "https://github.com/sijunyang/ding/releases/download/v0.1.0/ding-0.1.0-macos.zip"
  sha256 "PLACEHOLDER_SHA256_UPDATE_AFTER_RELEASE"
  license "MIT"
  version "0.1.0"

  on_macos do
    if Hardware::CPU.arm?
      def install
        bin.install "ding"
      end
    else
      def install
        bin.install "ding"
      end
    end
  end

  def install
    bin.install "ding"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/ding --version")
  end
end

Required fields present:
  desc "CLI tool that sends push notifications to iPhone/Apple Watch when terminal commands complete"
  homepage "https://github.com/sijunyang/ding"
  url "https://github.com/sijunyang/ding/releases/download/v0.1.0/ding-0.1.0-macos.zip"
  sha256 "PLACEHOLDER_SHA256_UPDATE_AFTER_RELEASE"
  license "MIT"
      def install
        bin.install "ding"
      def install
        bin.install "ding"
  def install
    bin.install "ding"
  test do
